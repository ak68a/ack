/**
 * Adds `toMatchSchema` matchers
 *
 * @example
 * ```ts
 * expect(value).toMatchSchema(schema)
 * expect(value).not.toMatchSchema(schema)
 * expect(value).toMatchSchema(schema, (parsed) => {
 *  // ... additional assertions
 * })
 * ```
 */
import "standard-parse/test-matchers/vitest"
